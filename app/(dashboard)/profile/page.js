export const metadata = {
    title: "AfriqLoan - User Profile",
  };

export default async function Profile(){
    return(
        <section className="p-5">
            <p>(Profile)</p>
        </section>
    )
}